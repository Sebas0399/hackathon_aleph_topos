{
  "name": "trazabilidad-filecoin",
  "version": "1.0.0",
  "description": "Sistema de trazabilidad en Filecoin",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy:calibration": "npx hardhat run scripts/deploy.js --network calibration",
    "deploy:mainnet": "npx hardhat run scripts/deploy.js --network filecoin"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.17.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.3",
    "dotenv": "^16.3.1"
  }
}